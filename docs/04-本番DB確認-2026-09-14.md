# 所在ダッシュボード 本番 DB 確認（2026-09-14）

場所: VPS `72.60.211.213` の `/opt/sakurai-dashboard/data/db.sqlite`（36KB、最終更新 2026-09-14 00:09）
コンテナ: `sd-api` / `sd-web` / `sd-cloudflared` が 2 か月稼働中（他サービスと同居。触らないこと）
確認方法: 読み取り専用で scp コピーして SQLite を開いた。本番には一切書き込んでいない。
バックアップ: `db.sqlite.bak-20260711-214452`、`db.sqlite.predeploy-20260711-221151` が同ディレクトリにある。

## テーブル

`staff` / `attachments` / `events` の 3 つ（ローカルの `app/data/db.sqlite` は古く `events` が無い。本番が正）。

## staff（6 名）

| id | 名前 | role | 備考 |
|---|---|---|---|
| S001 | 櫻井光明 | 代表 | |
| S002 | 富樫徹夫 | 技術 | |
| S003 | 千葉貴光 | 技術 | |
| S004 | 櫻井和俊 | 技術 | 予定件数が最多 |
| S005 | 倉西智樹 | 技術 | |
| **S006** | **櫻井会長** | 技術 | **会長限定アプリはこの行を使う** |

ローカルのシードデータ（桜井 健司 ほか 10 名）は本番とは別物。実装ではシードを見ない。

## events（57 件、2026-07-14 〜 2026-10-18）

実運用の傾向（AI の聞き取り設計に効く）
- **時刻は付けないことが多い**（57 件中 time あり 2 件）。「朝から」「終日」の感覚。
- **連日予定が多い**（`end_date` あり 17 件）。出張・工事は 2〜13 日。
- **title は「客先名」または「客先/作業/同行者」**の短い書き方。例: `菱和産業/粉体塗装設備デバック`、`モリタ 社･和･宮田･小島･宮下+1`、`富樫休み(歯医者定期健診)`
- kind の内訳: 工事 29 / 訪問 22 / 休み 3 / 社内 1 / その他 0
- 会長（S006）の予定: 8/16 モリタ（1名）訪問、8/20〜24 モリタ 工事（同行者付き）
- 添付は 0 件。

## お弁当について

**DB にお弁当の項目は無い。** `events.kind` も固定 5 種（訪問/工事/社内/休み/その他）で追加不可。
実装案（軽い順）:
1. スケジュールAI 側に小さな SQLite テーブル `lunch(date, staff_id, needed, answered_at)` を持つ ← **推奨**。ダッシュボードは無改修。
2. ダッシュボードの `Staff` に JSON 列を足す（改修と再デプロイが要る）。

お弁当ボード（事務用）はスケジュールAI 側の画面として作り、予定はダッシュボード API から読む。
